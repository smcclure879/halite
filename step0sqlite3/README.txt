i won't check this directory in. but here I built sqlite3 by hand. end up not needing this...

...instead...
 NOT SUDO   npm install -g sqlite3  --build-from-source

(or similar)   worked.

2nd try...
sudo npm install -g npm
sudo npm install -g ansi


npm install -g sqlite3  --build-from-source
